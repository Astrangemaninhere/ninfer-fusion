# 2026-09-01 修复与改动记录（合并项目素材）

## 1. E8Kv 精度根因修复（ppl 6.91 → 1.11）

根因（对照 PR #35 rk4v4-e8 实现）:
- 旧实现 E8 fill 既没有 Hadamard 旋转也没有 E8 格点投影（注释声称有，代码没有）
- scale 用 `/127`（int8 语义），而 4-bit code 范围是 ±7 → K/V 幅值缩小 18 倍
- 旋转域（无）与 g64 scale 域（64）不匹配

修复（`gqa_attention_prefill_i8.cuh` / `gqa_attention_decode_i8.cuh` 的 E8 路径）:
- fill: `gqa_kv_hadamard64(k0, k1)`（64 维 Sylvester，与 g64 scale 域对齐，
  函数从 PR #35 移植到 `gqa_attention_kv_quant.cuh`）
- fill: `e8_project_8d_warp` 投影后 rintf + clamp[-7,7]
- scale: `k_abs/7.0f`、`v_abs/7.0f`（不再是 /127）
- Q 侧: E8 时同样 `gqa_kv_hadamard64`（Q/K 同 R，点积语义保持）
- 邻居 nibble 同样走投影

验证（4096 ctx, perplexity-corpus）:
- all-E8: ppl 6.9144 → **1.1120**（NLL 0.1061）
- baseline（10L NVFP4 + 6L I8）: 1.5043（回滚补偿后待复测）

## 2. represented-scale 补偿（已回滚，待重做）

尝试把上游 nvfp4 的"E4M3 编码后反除"补偿合入 NVFP4 fill:
- baseline ppl 从 1.5043 恶化到 2.6526 → 回滚
- 原因待查：可能我们的手动 E4M3 编码与硬件 cvt 语义偏差，
  或补偿只做了 fill 端而 attention 端有隐含的 scale 用法差异
- 结论: 补偿方向保留在 ROADMAP，但需要先修好再合（上游分支也未经验证）

## 3. 残差平面崩溃（已知限制）

- 症状: `--kv-residual-layers` 配置下 block_tables 内容被破坏
  （fill 后 host 检查正常，attention 读垃圾）
- fill guard 全零触发（K/V code + scale 写均不越界）→ 破坏者不是 fill 的
  global 写；同一 stream 内 fill 后 attention 前无其他 kernel
- 已记录为已知限制（实验功能，E8Kv 主线修复优先）

## 4. 调试打印清理

- 移除 22 处 `[dbg*]` printf/fprintf（prefill_i8 / prefill_nvfp4 /
  launcher / prefill_e8）——4096 context 下 printf 拖慢 ppl 30 倍

## 5. fork 维护

- myfork/master 快进到上游 da49c0d
- 7 个功能分支 rebase 成功（issue142 / disk-cold-tier / pr1 / pr3 / yarn /
  pr4 / pr2）；nvfp4-cold-pool / local-kv-work-16 / nvfp4-nr-production
  保留旧版（冲突爆炸）
- E8Kv 工作树（ninfer-pr3）已 git init + commit 为 e8kv-current
  （push 待网络恢复）
