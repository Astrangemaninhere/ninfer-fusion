
# 待落地看板 v2 — 2026-09-10 15:2X 实况 (取代 v1 的 A–G 旧表)

口径: **已落地**=在被编译的树里且有证据; **已备**=补丁就绪待编译; **待验**=已编译待 serve/GPU 判定;
**未做**; **待决**; **阻塞**; **已陈旧**=原判有误。

## 今日的四个"状态纠正"（重要——v1 里有四条是错的）
| 原状态 | 实况 | 依据 |
|---|---|---|
| U1 `kKvFp8QuantGroup 256→16` **已落地** | **其实只在镜像里**；今日才拷进构建树 | B/S41 |
| U2 `E8Kv` workspace 白名单 **已落地** | 同上（只在镜像，今日拷回） | B/S41 |
| U3 `kv_options.h` 文档 **已落地** | 同上（只在镜像，今日拷回） | B/S41 |
| 256 修复"94 处已家族闭合" | **值对但实现不全**：3 处仍按旧宽度 4（copy 宽度 / cold 行 / 平面步长） | A/S42 |
另: `A_s36_headdim.diff` 现在**不可重放**（13/13 文件、53 hunk 全 FAIL）⇒ 只能从 live 树重派生。

## A. 256 步长家族 (Muse 正确性的核心)
| # | 项 | 状态 | 备注 |
|---|---|---|---|
| A1 | decode 索引 helper → `Geometry::HeadDim`（94 处） | **已落地**(window J) | qwen 256 路径 110592+27648 偏移逐字节不变 |
| A2 | i8 `Groups = D/kGqaKvQuantGroup` | **已落地**(我补) | 但见 A3/A4 |
| A3 | i8 copy 宽度 + `cold_i8_decode_row` | **已落地**(A/S42 补丁) | ⚠ **本轮二进制不含**（编译中途改头 ⇒ .o 比源新 ⇒ 需 `touch` 强制重编） |
| A4 | `gqa_attention_kv_quant.cuh` 平面 LeadingExtents（i8/E8 档在 Muse **整体 2×**） | **未做** | 必须与 `prefill_i8`（结构性 256-only）**成批**修 |
| A5 | nvfp4 decode 内核 Muse 拒绝（`if constexpr` + 响亮抛错） | **已落地**(window J) | 256 路径不变 |
| A6 | prefill 侧 nvfp4 同款守卫（4 attr + 4 launch） | **已落地**(我补) | 否则 prefill TU 编译必挂 |
| A7 | `prefill_i8` 结构性 256-only | **未做**(已标注) | 与 A4 同批 |

## B. KV / 分配 (原 N 系列)
| # | 项 | 状态 |
|---|---|---|
| B1 | N1 `--kv-bit-budget` 引擎入口 | **待验**(已编入本轮窗口) |
| B2 | N2 `--max-cold-pages` + 预算×冷耦合 | **待验**(同上) |
| B3 | N2 遗留 `ColdPolicy::Host` 死路径 | **待决**(接线 or 删) |
| B4 | N3 校准闭环: 侧车 + 加载器 + 身份门 + 采集接线(C/S38) | **待验**(补丁已应用, 需真数据) |
| B5 | N4 热/温/冷闭环: 策略(dry-only) + **F3b 缺陷** | **未做**: 空闲窗进 EWMA 无置信度门 ⇒ 真实能量不变的层被降级; 现提案关不掉 |
| B6 | N4 抖动 > 3600 s/h（设计级） | **未做**: 需修滞后/节奏, 否则引擎追不上自己 |
| B7 | N5=W13 权重卸载 P0 | **待验**(补丁已应用) |
| B8 | W13 P1 GEMM 钩子 | **未做** |
| B9 | §103 `sliding_window_tokens`（Muse SWA） | **待验**(S24 补丁已应用) |

## C. FlashNext / 导入
| # | 项 | 状态 |
|---|---|---|
| C1 | 契约(bindings) 74,804/74,804 缺失 0 | **已落地**(我独立复现) |
| C2 | 写出路径(重排版, 往返 12/12 逐字节) | **已落地**(S27) |
| C3 | 全量转换 | **阻塞**(下载 112/135GB, 今日重启过一次) |
| C4 | FP8 PLE 子布局钉死 | **阻塞**(分片未到齐) |
| C5 | 真运行时 registry+config（stage a） | **已备**(B/S37 补丁未应用) |
| C6 | MoE 512 专家路由算子 | **未做**(最大缺口) |
| C7 | GDN/hyper-connection 层 | **未做** |
| C8 | MTP 27 张量 | **未做** |
| C9 | LFM2 适配（no-SiLU/宽3 变体 + 两道门） | **规格已定**(§125) |
| C10 | Falcon-H1R（Mamba/SSD） | **未做**(大件) |

## D. 引擎鲁棒性
| # | 项 | 状态 |
|---|---|---|
| D1 | W16 残留四修（含 /health 脱敏） | **待验** |
| D2 | W16 §3.4 校验前移 + 静态闸门 | **待验** |
| D3 | W16 四道验收门（req-1 失败 req-2 正常） | **未做**(需服务) |

## E. dflash2 / 接受率（用户主张: 非训练问题）
| # | 项 | 状态 |
|---|---|---|
| E1 | 机制判定: **训练目标疑似整体后移一位**（`ids16[a+1+i]` vs 推断的 `a+i`） | **未决**: 理论论证强、样本太小(112 对, 11.6% vs 15.2%)、且我的判定逻辑有 bug(已修) |
| E2 | 隔离 A/B（只改 `--target-shift`，1 vs 0） | **未做**: 脚本就绪(`_df2_shift_ab.bat`)，两次都因显存上限/重启中断 |
| E3 | 跨 ckpt 趋势（200/1200/6000 的 hit@1） | **部分**: 1200=0.2719, 200 的不可比 |
| E4 | 训练到 6000 步 | **未做**(按用户要求放最后) |
| E5 | 树的真实收益（离线 +0.1133 token/块；ORACLE-GAP +0.0686） | **待 GPU 复测** |

## F. 构建效率
| # | 项 | 状态 |
|---|---|---|
| F1 | L1: 22GB 上限 + split-compile + -j1 + 重试 | **已落地**(经受住两次重启) |
| F2 | L2 ccache 入口 | **已落地**(本轮已在用) |
| F3 | L3 按档拆 TU | **已备**(C/S40 v2, 33/33 验证) 但**未应用**；且必须从 live 树重派生(镜像那份会回退守卫) |
| F4 | 镜像↔构建树对账 | **已做**(B/S41) + 3 项已导入；余 `qpn_host.cu` 注册 / `-rdynamic` / perplexity chunk **待决** |
