
## 用户追加铁律（2026-09-11 14:2X，长期有效）
⑦ **绝不扫盘**：任何 `ls`/`grep -r`/`find` 式的全盘或目录扫描都被禁止（太慢）。
   只允许：① 读写**已知确切路径**的单个文件；② 用会话/自己写的报告里的既有记录；
   ③ 需要新信息时**直接问用户要指针**（文件名/行/命令）。
   例外：单文件内的 grep（已知路径）允许。

## 关键更正（用户，2026-09-11）
- 干净环境那套 vLLM（`C:\vllm\venv-clean`，vllm 0.26.0）**是最新版、带 dflash2 支持**，
  那次跑的就是 **dflash2**，且**接受率正常**。
  ⇒ 同一 ckpt + 同一草稿在独立实现上正常 ⇒ **差异在 ninfer 的管线**，而不是草稿质量。
  （我此前"两次 vLLM 都塌陷"的记录需按此更正；`dl/clean_run.log` 里那条 `method="dspark"`
  属于另一次运行，不能当 dflash2 的对照。）
- ⇒ 审计优先级：**参照实现的 dflash2 管线参数**（feature 捕获/context K/V/块构造/positions/
  verify/状态更新）vs ninfer 的实现。

## 用户追加铁律（P7，2026-09-11 18:1X，长期有效）
- **修复必须双路取证**：同一修复任务派**两个** agent、用**同样的方法**各做一遍（注重计算机数据/量化证据，不是叙述）；主代理**对比两份 → 求其同 → 分析不同 → 最后落地**。
- **不许拿上下文当停工理由**：上下文与是否继续工作无关。

## 用户定下的长期优先级（2026-09-11 16:1X，长期有效）
- **重训练永远最低权重** ⇒ 抬接受率/吞吐只能靠引擎侧；ckpt 线降为最低优先级。
- **合并 = 统一算术**：让 plain(T=1) 与 verify(T=W) 在每个 T 分派点走同一 kernel/累加顺序/精度档。
- 性能优先的取舍原则：宁可让 T=1 改走 T∈[2,16] 的 kernel，也不许 verify 退化/串行化/大缓冲升 FP32。
